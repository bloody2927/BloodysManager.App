<Window x:Class="BloodysManager.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:BloodysManager.App.Views.Converters"
        Title="{Binding AppTitle}"
        MinWidth="960"
        MinHeight="640"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <conv:BoolToBrushConverter x:Key="BoolToBrush" />
    </Window.Resources>

    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Datei">
                <MenuItem Header="{Binding BtnCreateStruct}" Command="{Binding CmdCreateFolders}" />
                <Separator />
                <MenuItem Header="{Binding BtnExit}" Command="{Binding CmdExit}" />
            </MenuItem>
            <MenuItem Header="Settings" Command="{Binding CmdOpenSettings}" />
            <MenuItem Header="Credits" Command="{Binding CmdOpenCredits}" />
        </Menu>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24" HorizontalAlignment="Stretch">
                <TextBlock Text="{Binding AppTitle}"
                           FontSize="32"
                           FontWeight="Bold"
                           HorizontalAlignment="Center" />

                <Button Content="{Binding BtnCreateStruct}"
                        Command="{Binding CmdCreateFolders}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        HorizontalAlignment="Center" />

                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="Pfad Übersicht" Style="{StaticResource SectionHeaderTextStyle}" />

                        <Grid Margin="0,4,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Ellipse Style="{StaticResource StatusDotStyle}"
                                     Fill="{Binding ExistsDownload, Converter={StaticResource BoolToBrush}}" />
                            <TextBlock Grid.Column="1" Text="Download" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBox Grid.Column="2"
                                     Style="{StaticResource PathTextBoxStyle}"
                                     Text="{Binding PathDownload, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>

                        <Grid Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Ellipse Style="{StaticResource StatusDotStyle}"
                                     Fill="{Binding ExistsLive, Converter={StaticResource BoolToBrush}}" />
                            <TextBlock Grid.Column="1" Text="Live" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBox Grid.Column="2"
                                     Style="{StaticResource PathTextBoxStyle}"
                                     Text="{Binding PathLive, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>

                        <Grid Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Ellipse Style="{StaticResource StatusDotStyle}"
                                     Fill="{Binding ExistsCopy, Converter={StaticResource BoolToBrush}}" />
                            <TextBlock Grid.Column="1" Text="Copy" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBox Grid.Column="2"
                                     Style="{StaticResource PathTextBoxStyle}"
                                     Text="{Binding PathCopy, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>

                        <Grid Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Ellipse Style="{StaticResource StatusDotStyle}"
                                     Fill="{Binding ExistsBackup, Converter={StaticResource BoolToBrush}}" />
                            <TextBlock Grid.Column="1" Text="Backup" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBox Grid.Column="2"
                                     Style="{StaticResource PathTextBoxStyle}"
                                     Text="{Binding PathBackup, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>

                        <Grid Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Ellipse Style="{StaticResource StatusDotStyle}"
                                     Fill="{Binding ExistsBackupUpdate, Converter={StaticResource BoolToBrush}}" />
                            <TextBlock Grid.Column="1" Text="Backup Zip" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBox Grid.Column="2"
                                     Style="{StaticResource PathTextBoxStyle}"
                                     Text="{Binding PathBackupUpdate, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <UniformGrid Columns="4" Margin="0,8,0,0">
                    <Button Content="{Binding BtnClean}"
                            Style="{StaticResource RoundedActionButtonStyle}"
                            Background="{StaticResource AccentOrange}"
                            Command="{Binding CmdCleanDownload}" />
                    <Button Content="{Binding BtnUpdate}"
                            Style="{StaticResource RoundedActionButtonStyle}"
                            Background="{StaticResource AccentOrange}"
                            Command="{Binding CmdUpdate}" />
                    <Button Content="{Binding BtnCopy}"
                            Style="{StaticResource RoundedActionButtonStyle}"
                            Background="{StaticResource AccentGreen}"
                            Command="{Binding CmdCopy}" />
                    <Button Content="{Binding BtnDeleteLive}"
                            Style="{StaticResource RoundedActionButtonStyle}"
                            Background="{StaticResource AccentRed}"
                            Command="{Binding CmdDeleteLive}" />
                </UniformGrid>

                <UniformGrid Columns="3" Margin="0,8,0,0">
                    <Button Content="{Binding BtnDeleteCopy}"
                            Style="{StaticResource RoundedActionButtonStyle}"
                            Background="{StaticResource AccentRed}"
                            Command="{Binding CmdDeleteCopy}" />
                    <Button Content="{Binding BtnRotate}"
                            Style="{StaticResource RoundedActionButtonStyle}"
                            Background="{StaticResource AccentGold}"
                            Command="{Binding CmdRotate}" />
                    <Button Content="{Binding BtnExit}"
                            Style="{StaticResource RoundedActionButtonStyle}"
                            Background="{StaticResource AccentGold}"
                            Command="{Binding CmdExit}" />
                </UniformGrid>

                <Border Style="{StaticResource CardBorderStyle}" Margin="0,8,0,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                            <TextBlock Text="Status:" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding IsBusy}" Margin="8,0,0,0" />
                        </StackPanel>
                        <TextBlock Text="Log" Margin="0,8,0,4" FontWeight="SemiBold" />
                        <Border BorderBrush="#FFCCCCCC" BorderThickness="1" CornerRadius="6">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="200">
                                <TextBox Text="{Binding Log}"
                                         IsReadOnly="True"
                                         TextWrapping="Wrap"
                                         FontFamily="Consolas"
                                         FontSize="12"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         AcceptsReturn="True" />
                            </ScrollViewer>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
